const Board = {
    checkWinner: (board) => {},
    drawBoard: {}
}

const Player = {
    play: (row, column) => {}
}

const Classes = {
    startGame: () => {},
    endGame: () => {},
    addToScore: () => {},
    getScore: () => {}
}

