const Board = {
    checkWinner: (board) => {},
    drawBoard: {}
}

const Player = {
    play: (row, column) => {}
}

const Game = {
    startGame: () => {},
    endGame: () => {},
    addToScore: () => {},
    getScore: () => {}
}

